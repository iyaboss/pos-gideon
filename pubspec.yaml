name: pos_gideon
description: Aplikasi POS Toko Gideon - Codemagic Ready
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.0
  firebase_analytics: ^10.8.0
  provider: ^6.1.0
  bluetooth_print: ^4.3.0
  flutter_bluetooth_serial: ^0.4.0
  intl: ^0.18.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  fl_chart: ^0.66.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
